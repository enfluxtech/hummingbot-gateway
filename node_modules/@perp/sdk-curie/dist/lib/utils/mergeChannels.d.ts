import { EventHandler } from "../internal";
import { Channel } from "../internal/Channel";
export declare function mergeChannels(channels: Channel[]): {
    on: (eventName: string, handler: EventHandler) => () => void;
} | null;
