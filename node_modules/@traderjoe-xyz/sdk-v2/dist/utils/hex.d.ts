import { CurrencyAmount } from '@traderjoe-xyz/sdk';
export declare function validateAndParseAddress(address: string): string;
/**
 * Converts currency amount into hex encoding
 *
 * @param {CurrencyAmount} currencyAmount
 * @returns {string}
 */
export declare function toHex(currencyAmount: CurrencyAmount): string;
/**
 * Returns true if the string value is zero in hex
 *
 * @param {string} hexNumberString
 * @returns {boolean}
 */
export declare function isZero(hexNumberString: string): boolean;
