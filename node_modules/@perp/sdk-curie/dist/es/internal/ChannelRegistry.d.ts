import { Channel } from "./Channel";
export declare class ChannelRegistry<EventName extends string = string> {
    private _members;
    register(instance: Channel<EventName>): void;
    unregister(instance: Channel<EventName>): void;
    cleanUp(): void;
}
