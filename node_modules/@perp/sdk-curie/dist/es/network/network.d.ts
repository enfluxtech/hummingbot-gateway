import { providers } from "ethers";
import { RetryProvider } from "./RetryProvider";
export declare function getProvider({ rpcUrl }: {
    rpcUrl: string;
}): providers.StaticJsonRpcProvider;
export interface ProviderConfig {
    rpcUrl: string;
}
export declare function getRetryProvider(providerConfigs: ProviderConfig[]): RetryProvider;
